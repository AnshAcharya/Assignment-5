#include <stdio.h>

int check_unique(char str[], int start, int end)
{
    
}

int main()
{
    int n, k;
    scanf("%d %d", &n, &k);
    char str[n + 1];
    scanf("%s", str);
    //printf("%s\n", str);
    for (int i = 0; i < n; i++)
    {
        for (int j = i; j < n; j++)
        {
               
        }
    }
    return 0;
}